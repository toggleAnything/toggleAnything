import React from 'react';
import ToggleOnIcon from '@material-ui/icons/ToggleOn';

const sideBar = [
  {
    icon: <ToggleOnIcon />,
    link: '/toggle',
    text: 'Toggles',
  },
];

export default sideBar;
